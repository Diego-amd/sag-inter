@model List<ProdutosMaisVendidos>
@{
    ViewBag.Title = "Vendas Do Dia";
    ViewBag.NomePagina = "Relatório de vendas do dia";
}
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">

<head>
    <link rel="stylesheet" href="~/css/relatorios/vendasdodia.css">
</head>
<body class="vendas">
    <div class="msg1">
        <h1 id="msg1">Vendas Do Dia 🛒</h1>
    </div>
    <table class="tabela">
        <tr>
            <th>Produto</th>
            <th>Valor Unitário</th>
            <th>Valor Total Vendido</th>
            <th>Qtd Total em Pedidos</th>
            <th>Valor médio por pedido</th>
        </tr>

        @if(ViewBag.Mensagem != null) {
            <p class="alert alert-danger alert-message">@ViewBag.Message</p>
        }

        @foreach(var produtomv in Model)
        {
            <tr>
                <td class="tabela-info-td">@produtomv.Nome</td>
                <td class="tabela-info-td">@produtomv.ValorUnitario.ToString("C")</td>
                <td class="tabela-info-td">@produtomv.ValorTotal.ToString("C")</td>
                <td class="tabela-info-td">@produtomv.Qtde</td>
            </tr>
        }
        <tr>
            <td colspan="4" class="tabela-info-td">@ViewBag.Media.ToString("C")</td>
        </tr>
    </table>
</body>

